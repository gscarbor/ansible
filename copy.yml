hosts:  all
  gather_facts: False
  remote_user: solise@ferc.gov
  become: yes
  tasks:
     - name: Copy a file to remote servers
       become: true
       copy:

               src: /uploads/azure_fasttrack.sh
               dest: /home/solise@ferc.gov/azure_fasttrack.sh
               owner: root
               group: root
