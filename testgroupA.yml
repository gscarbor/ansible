---
- hosts: all
  gather_facts: false
  tasks:
  - debug:
      var: subgroup
  - name: set variable
    ansible.builtin.set_stat:
      data:
        subgroup: GroupA
       
  - debug: 
      var: subgroup
 
