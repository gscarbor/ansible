---
- hosts: all
  gather_facts: false
  tasks:
  - debug:
      var: subgroup
  
  - name: set variable
    ansible.builtin.set_fact:
      subgroup: GroupB
       
  - debug: 
      var: subgroup
 
