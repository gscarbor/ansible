- name: Listen for events on kafka
  hosts: all

  sources:
    - ansible.eda.kafka:
        host: ansible.local.net
        port: 9092
        topic: kafka-test-topic
        #group_id: ansible-eda
    
  rules:
    - name: kafka
      condition: event.message != ""
      action:
        debug:
          msg: "You are good!"
        
